<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.upc.agnosticsix.goodcow">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/goodcow"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BovinoActivity" />
        <activity android:name=".DetallesActivity" />
        <activity android:name=".PalpamientoActivity" />
        <activity android:name=".VacunasActivity" />
        <activity android:name=".CruzamientoActivity" />
        <activity android:name=".OrdenhaActivity" />
        <activity android:name=".AcercaDeActivity" />
        <activity android:name=".DecesosActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" />
        <activity android:name=".ZoometricasActivity" />
        <activity android:name=".ZoometricaDetalleActivity"></activity>
    </application>

</manifest>